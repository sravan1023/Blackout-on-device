<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- No INTERNET permission — zero-trust, fully offline -->

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".BlackoutApp"
        android:allowBackup="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blackout">

        <!-- DSP RPC — required for Qualcomm HTP (Hexagon) FastRPC to the CDSP -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <!-- OpenCL — without these two declarations the vendor linker namespace blocks dlopen
             for libvndksupport.so, causing LiteRT to fall back from OpenCL → OpenGL, where
             CreateSharedMemoryManager is unimplemented on Android 16 (gpu_backend_opengl.cc:169).
             Declaring them required="false" opens the namespace slot without hard-failing on
             devices that lack OpenCL (e.g. emulators). -->
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".benchmark.BenchmarkReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.blackout.BENCHMARK" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Blackout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
